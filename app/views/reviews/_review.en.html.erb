<div class="review-rating" data-score="<%= review.rating %>"><label><h4>Rating:&nbsp;</h4></label></div>
<h6 style="visibility:hidden">Rating number:&nbsp;<%= review.rating %>:&nbsp;Stars</h6>
<h4>Comment:&nbsp;<%= review.comment %></h4>

<h4>Posted by User:&nbsp;<b><%= review.user.username %></b></h4>

<div class="form-group col-md-6">
<% if user_signed_in? %>

	<% if review.user_id == current_user.id %>			
		<%= link_to "Edit", edit_app_review_path(review.app, review)%>
		<%= link_to "Delete", app_review_path(review.app, review), method: :delete, data: { confirm: "Are you sure?" }%>
	<% end %>	

<% end %>
</div>
<br/>			