<%= simple_form_for(@tab) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :infromation %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
