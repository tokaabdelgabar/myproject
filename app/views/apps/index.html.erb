
 <br></br>
 <p><a href="blind" target="_blank" class="btn btn-primary btn-lg" style="margin-left:900px; margin-top: 1px;">See More</a></p>

<% if params[:category].blank? && params[:search].blank? %>

	<div class="row">
	<b><h1 style="font-size:110%;"><%=t :Blind %></h1></b>
	<div class="app">
		<% @apps_blind.limit(5).each do |app| %>
			<div class="col-md-2">
			<a href="/apps/<%= app.id %>"><%= image_tag app.app_img.url(:app_index), class: "app_image" %></a>
			<p><%= link_to app.name, app_path(app) %></p>
			<p class="app-name"><%= app.price %></p>
			<div class="average-review-rating" data-score=<%= app.reviews.average(:rating) %>></div>
			</div>
		<% end %>
	</div>
	</div>

	 <br></br>

	<p><a href="sight" target="_blank" class="btn btn-primary btn-lg" style="margin-left:900px; margin-top: 1px;">See More</a></p>


	<div class="row">
	<b><h1 style="font-size:110%;"><%=t :Sight_Impaired %></h1></b>
	<div class="app">
		<% @apps_sight.limit(5).each do |app| %>
			<div class="col-md-3">
			<a href="/apps/<%= app.id %>"><%= image_tag app.app_img.url(:app_index), class: "app_image" %></a>
			<p><%= link_to app.name, app_path(app) %></p>
			<p class="app-name"><%= app.price %></p>
			<div class="average-review-rating" data-score=<%= app.reviews.average(:rating) %>></div>
			</div>
			
		<% end %>
	</div>
	</div>

 <br></br>

<p><a href="motorical" target="_blank" class="btn btn-primary btn-lg" style="margin-left:900px; margin-top: 1px;">See More</a></p>
<div class="row">
	<b><h1 style="font-size:110%;"><%=t :Motorically_Impaired %></h1></b>
	<div class="app">
		<% @apps_motorically.limit(5).each do |app| %>
			<div class="col-md-3">
			<a href="/apps/<%= app.id %>"><%= image_tag app.app_img.url(:app_index), class: "app_image" %></a>
			<p><%= link_to app.name, app_path(app) %></p>
			<p class="app-name"><%= app.price %></p>
			<div class="average-review-rating" data-score=<%= app.reviews.average(:rating) %>></div>
			</div>
			
		<% end %>
	</div>
	</div>
 <br></br>

<p><a href="learning" target="_blank" class="btn btn-primary btn-lg" style="margin-left:900px; margin-top: 1px;">See More</a></p>
<div class="row">
	<b><h1 style="font-size:110%;"><%=t :Learning_Impaired %></h1></b>
	<div class="app">
		<% @apps_learning.limit(5).each do |app| %>
			<div class="col-md-3">
			<a href="/apps/<%= app.id %>"><%= image_tag app.app_img.url(:app_index), class: "app_image" %></a>
			<p><%= link_to app.name, app_path(app) %></p>
			<p class="app-name"><%= app.price %></p>
			<div class="average-review-rating" data-score=<%= app.reviews.average(:rating) %>></div>
			</div>
			
		<% end %>
	</div>
	</div>
 <br></br>

<p><a href="store#index" target="_blank" class="btn btn-primary btn-lg" style="margin-left:900px; margin-top: 1px;">See More</a></p>
<div class="row">
	<b><h1 style="font-size:110%;"><%=t :Hearing_Impaired %></h1></b>
	<div class="app">
		<% @apps_deaf.limit(5).each do |app| %>
			<div class="col-md-3">
			<a href="/apps/<%= app.id %>"><%= image_tag app.app_img.url(:app_index), class: "app_image" %></a>
			<p><%= link_to app.name, app_path(app) %></p>
			<p class="app-name"><%= app.price %></p>
			<div class="average-review-rating" data-score=<%= app.reviews.average(:rating) %>></div>
			</div>
			
		<% end %>
	</div>
	</div>

 <br></br>

<p><a href="store#index" target="_blank" class="btn btn-primary btn-lg" style="margin-left:900px; margin-top: 1px;">See More</a></p>
<div class="row">
	<b><h1 style="font-size:110%;"><%=t :WheelChair %></h1></b>
	<div class="app">
		<% @apps_whealchair.limit(5).each do |app| %>
			<div class="col-md-3">
			<a href="/apps/<%= app.id %>"><%= image_tag app.app_img.url(:app_index), class: "app_image" %></a>
			<p><%= link_to app.name, app_path(app) %></p>
			<p class="app-name"><%= app.price %></p>
			<div class="average-review-rating" data-score=<%= app.reviews.average(:rating) %>></div>
			</div>
			
		<% end %>
	</div>
</div>

<% elsif !params[:category].blank? && params[:search].blank? %>

<div class="row">
	<div class="app">
		<% @apps.each do |app| %>
			<div class="col-md-3">
				<a href="/apps/<%= app.id %>"><%= image_tag app.app_img.url(:app_index), class: "app_image" %></a>
			<p><%= link_to app.name, app_path(app) %></p>
			<p class="app-name"><%= app.price %></p>
			<div class="average-review-rating" data-score=<%= app.reviews.average(:rating) %>></div>
			</div>	
		<% end %>
	</div>
	</div>

<% elsif %>
<div class="row">
	<div class="app">
		<% @apps.each do |app| %>
			<div class="col-md-3">
				<a href="/apps/<%= app.id %>"><%= image_tag app.app_img.url(:app_index), class: "app_image" %></a>
			<p><%= link_to app.name, app_path(app) %></p>
			<p class="app-name"><%= app.price %></p>
			<div class="average-review-rating" data-score=<%= app.reviews.average(:rating) %>></div>
			</div>	
		<% end %>
	</div>
	</div>
<% end %>

  <!-- FOOTER -->
   	  <br></br>
      <br></br>
      <br></br>
      <footer align="center">
        <p class="pull-right"><a href="#"><%= image_tag("top.gif", :alt => "Back to top", :size=> "130x100" ) %></a></p>
        <p>&copy; 2016 Company, Inc. &middot; <a href="#">Privacy</a> &middot; <a href="#">Terms</a></p>
      </footer>

<br></br>
      <br></br>
      <br></br>




<script>
	$('.average-review-rating').raty({
		readOnly: true,
		path: '/assets/',
		score: function() {
			return $(this).attr('data-score')
		}
	});
</script>





